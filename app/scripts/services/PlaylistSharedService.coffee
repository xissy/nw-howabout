
howaboutServices.factory 'PlaylistSharedService', [
  '$rootScope'
  ($rootScope) ->
    sharedService =
      playlist: []
      playingIndex: null

      playTrack: (track) ->

      pause: ->

      playPrev: ->

      playNext: ->

      playIndex: (index) ->

      addNext: (track) ->

      addLast: (track) ->

      
      
]
